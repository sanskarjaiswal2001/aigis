#!/bin/bash
# Clear disk cache (placeholder). Allowlisted script for aigis.
# In production, implement appropriate cache clearing (e.g. drop caches with privileges).
set -e
echo "clear_disk_cache: placeholder - no-op"
